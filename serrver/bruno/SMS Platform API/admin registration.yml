info:
  name: admin registration
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  body:
    type: json
    data: |-
      {
        "firstName": "Admin",
        "lastName": "User",
        "email": "uchegospel2412@gmail.com",
        "password": "Admin1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
