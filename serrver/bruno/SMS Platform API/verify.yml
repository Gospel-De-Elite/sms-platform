info:
  name: verify
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/auth/verify-email
  body:
    type: json
    data: |-
      {
        "email": "keguv2001@gmail.com",
        "otp": "891614"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
