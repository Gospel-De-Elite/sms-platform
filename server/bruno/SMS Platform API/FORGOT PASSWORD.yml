info:
  name: FORGOT PASSWORD
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:5000/api/auth/forgot-password
  body:
    type: json
    data: |-
      {
        "email": "keguv2001@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
